{
 "started_utc": "2026-09-16T13:57:44.174465+00:00",
 "wall_seconds": 17.09857835299772,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:58:01.273190+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 62,
  "temperature_c": {
   "min": 65.0,
   "max": 74.0,
   "mean": 71.75806451612904
  },
  "power_w": {
   "min": 117.56,
   "max": 268.81,
   "mean": 242.97096774193548
  },
  "util_pct": {
   "min": 0.0,
   "max": 95.0,
   "mean": 80.88709677419355
  },
  "clock_sm_mhz": {
   "min": 1710.0,
   "max": 1935.0,
   "mean": 1907.4193548387098
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "7f5dffa96516950cdb65ed0d6dae1c0ad8266dfb84ef51924469bba5d9df7af4",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --re.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-by.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE +.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, v.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md      .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101 --repeats 3\n```\n\n`scripts/serve.sh` prints `launch accepted` when the server is up and the PLE table is loaded. The\n`--max-running-requests 1` in it is deliberate: the server's `ple_max_ngram_bytes` (1.0 GB) is the\nPLE n-gram cache budget, and two concurrent requests would need 1.2 GB, which the server rejects at\nstartup. The server's `--ple-max-ngram-bytes` flag is the knob for concurrency; `--max-running-requests`\nis the knob for the number of requests the server runs at once.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, the serving patch, the KV-tier and elastic-cache patches, the PLE patches\nkernels/        the Triton kernels: int2_moe.py (int2 MoE), int2_moe_ple.py (int2 MoE + PLE), int2_moe_ple.py.orig\ntools/          bench.py, ple_probe.py, vmm_probe.py, vmm_probe.c, vmm_probe.py.orig, vmm_probe.c.orig\nscripts/        serve.sh, serve.sh.orig, serve.sh.orig2, serve.sh.orig3, serve.sh.orig4, serve.sh.orig5, serve.sh.orig6\ndocs/           CAMPAIGN.md (the engineering log), DESIGN.md (the design documents), logs/ (the dated logs)\nassets/         phase1_state.json (the accepted state), phase1_state.json.orig, phase1_state.json.orig2\nREADME.md       this file\nLICENSE         Apache-2.0 (code)\n```\n\n## What is in the patch\n\nThe patch is 1,050 lines over 13 files at SGLang `73a255206f`. It adds the Qwen",
 "ttft_seconds": 3.0936937909937114,
 "status": "ok",
 "tg_tokens_per_s": 36.48753460195983,
 "decode_seconds_per_token": 0.027406620121335563,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.190050001081545,
  "p95": 33.959374006371945,
  "mean": 27.406620121335564
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
