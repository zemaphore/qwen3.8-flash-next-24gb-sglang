{
 "started_utc": "2026-09-16T13:58:01.276274+00:00",
 "wall_seconds": 14.854868624002847,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:58:16.131255+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 74.81132075471699
  },
  "power_w": {
   "min": 253.94,
   "max": 270.78,
   "mean": 265.431320754717
  },
  "util_pct": {
   "min": 86.0,
   "max": 98.0,
   "mean": 90.54716981132076
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1920.0,
   "mean": 1901.3207547169811
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "3a0ece0d769799a7a392155a8aaa3dcebbd4326d5ac0a091b659a3943e0b6ae0",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthrough.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the server's own `ple.json`. The full\ncommand line, including the `--ple-emb-table-path` and `--ple-emb-table-json` arguments, is printed by\n`serve.sh` and recorded in `docs/logs/night.log`.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers, as applied to the SGLang checkout\nkernels/        the Triton kernels the patch loads (int2 expert GEMM, INT8 KV ring, PLE embedding)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and serve scripts\ntools/        the measurement tools: bench.py, ple.py, kv_tiers.py, kv_tiers.sh, kv_tiers_check.py,\n              kv_tiers_quality.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n              kv_tiers_quality_check.py, kv_tiers_quality.sh, kv_tiers_quality_check.py, kv_tiers_quality.sh,\n",
 "ttft_seconds": 1.3625683179998305,
 "status": "ok",
 "tg_tokens_per_s": 37.8736593562249,
 "decode_seconds_per_token": 0.026403574859096376,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.081503005116247,
  "p95": 32.50282799854176,
  "mean": 26.403574859096377
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
