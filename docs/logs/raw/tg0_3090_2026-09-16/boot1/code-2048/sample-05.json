{
 "started_utc": "2026-09-16T13:10:18.413679+00:00",
 "wall_seconds": 16.738227863002976,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:10:35.151969+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 60,
  "temperature_c": {
   "min": 79.0,
   "max": 81.0,
   "mean": 80.0
  },
  "power_w": {
   "min": 253.93,
   "max": 274.2,
   "mean": 262.7651666666667
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 91.51666666666667
  },
  "clock_sm_mhz": {
   "min": 1695.0,
   "max": 1905.0,
   "mean": 1892.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "bbdaf88f6f0e2995bfc7c936873ae543e015babbf5d85b89251d70e6a3976e19",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the var+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # padd+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs +=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Un+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 <<+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter))+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b = b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b = b.reshape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b = b.reshape(B+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b = b.reshape(BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # paddings of K.\n+        # Only load A on the \"real\" K, so we don't load out of bounds.\n+        ka = k\n+        kb = k\n+        if not even_Ks:\n+            a_real_k = min(ka, K - BLOCK_SIZE_K)\n+            ka = a_real_k\n+        kb = min(kb, K - BLOCK_SIZE_K)\n+\n+        a_ptrs += BLOCK_SIZE_K * stride_ak\n+        # Mask of cols for this iteration in the K loop\n+        mask_a_k = ka + tl.arange(0, BLOCK_SIZE_K) < K\n+        acc_a_ptrs = a_ptrs - BLOCK_SIZE_K * stride_ak + ka * stride_ak\n+        a = tl.load(\n+            acc_a_ptrs,\n+            mask=token_mask[:, None] & mask_a_k[None, :],\n+            other=0.0,\n+        )\n+        b = tl.load(b_ptrs)\n+        if use_int2_w2a16:\n+            # Unpack 16 values from each int32 word: (BLOCK_SIZE_K//16, BLOCK_SIZE_N, 16)\n+            b = (b // (1 << b_shifter)) & 0x3\n+            b = b.reshape(BLOCK_SIZE",
 "ttft_seconds": 1.1638511889977963,
 "status": "ok",
 "tg_tokens_per_s": 32.81041272542113,
 "decode_seconds_per_token": 0.030478129256362917,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 29.884490002586972,
  "p95": 38.709885004209355,
  "mean": 30.478129256362916
 },
 "class": "code",
 "requested_input_tokens": 2048
}
