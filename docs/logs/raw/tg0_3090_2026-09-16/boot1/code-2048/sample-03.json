{
 "started_utc": "2026-09-16T13:09:45.403000+00:00",
 "wall_seconds": 15.950753340999654,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:10:01.353826+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 57,
  "temperature_c": {
   "min": 79.0,
   "max": 80.0,
   "mean": 79.6842105263158
  },
  "power_w": {
   "min": 253.96,
   "max": 274.03,
   "mean": 263.8640350877193
  },
  "util_pct": {
   "min": 83.0,
   "max": 100.0,
   "mean": 89.71929824561404
  },
  "clock_sm_mhz": {
   "min": 1590.0,
   "max": 1905.0,
   "mean": 1882.6315789473683
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "39811425af371d5d623a614f4bc88276112c5235e192fd38035ade380dd44c28",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) %+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+               +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+    elif has_zp and use_int4_w4a16:\n+        b_zp_shifter = (offs_bn[1, None] % 2) * 4\n+    elif has_zp and use_int8_w8a16:\n+        b_zp_shifter = tl.arange(0, BLOCK_SIZE_N) % 2\n+\n+    if use_int2_w2a16:\n+        # scales/zp live in the word domain: stride_bsk is in words, one word covers\n+        # 16 consecutive K values (group_size must be >= BLOCK_SIZE_K).\n+        tl.static_assert(\n+            group_size >= BLOCK_SIZE_K,\n+            \"int2 word path needs group_size >= BLOCK_SIZE_K\",\n+        )\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[None, :]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + scale_offs[None, :]\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+    else:\n+        scale_offs = (offs_k // group_size) * stride_bsk\n+        scale_ptrs = (\n+            b_scale_ptr\n+            + off_experts * stride_bse\n+            + scale_offs[:, None]\n+            + offs_bn[None, :] * stride_bsn\n+        )\n+        if has_zp:\n+            zp_ptrs = (\n+                b_zp_ptr\n+                + off_experts * stride_bze\n+                + (offs_k // group_size) * stride_bzk\n+                + offs_bn[None, :] * stride_bzn\n+            )\n+\n+    for k in",
 "ttft_seconds": 1.1838942199974554,
 "status": "ok",
 "tg_tokens_per_s": 34.60462629283813,
 "decode_seconds_per_token": 0.02889787023092236,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 28.37072900001658,
  "p95": 37.536996002017986,
  "mean": 28.89787023092236
 },
 "class": "code",
 "requested_input_tokens": 2048
}
