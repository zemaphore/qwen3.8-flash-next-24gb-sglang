{
 "started_utc": "2026-09-16T13:12:42.487946+00:00",
 "wall_seconds": 30.548277850997692,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:13:13.036320+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 109,
  "temperature_c": {
   "min": 79.0,
   "max": 81.0,
   "mean": 80.72477064220183
  },
  "power_w": {
   "min": 249.85,
   "max": 281.87,
   "mean": 265.0821100917431
  },
  "util_pct": {
   "min": 55.0,
   "max": 100.0,
   "mean": 93.44954128440367
  },
  "clock_sm_mhz": {
   "min": 1125.0,
   "max": 1905.0,
   "mean": 1742.6146788990825
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "386c32388de84c2c6ce5dfb881a05d06dd346e69c39f73f0e85162e258c7e3c3",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow S+        \"\"\"Grow S until+        \"\"\"Grow S until the+        \"\"\"Grow S until the driver+        \"\"\"Grow S until the driver's+        \"\"\"Grow S until the driver's free+        \"\"\"Grow S until the driver's free VR+        \"\"\"Grow S until the driver's free VRAM+        \"\"\"Grow S until the driver's free VRAM drops+        \"\"\"Grow S until the driver's free VRAM drops to+        \"\"\"Grow S until the driver's free VRAM drops to reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S <+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free <+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S +=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step;+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\",+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VR+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S >+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except Arena+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+               +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S +=+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step;+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\",+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb /+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 10+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 102+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_reg+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _reg+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink,+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free <+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.reg+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve <<+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 2+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+           +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S =+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n++        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+       +        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S <+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S < self+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S < self.E+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+        \"\"\"Grow S until the driver's free VRAM drops to reserve_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < reserve_mb << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free < reserve_mb << 20:\n+                break\n+            S += step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S -= step; break\n+        logger.info(\"elastic fill: S=%d (reserve %d MB, free %.2f GB)\", S, reserve_mb, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, target_mb, step=8):\n+        \"\"\"Shrink S until the driver's free VRAM reaches target_mb.\"\"\"\n+        torch.cuda.empty_cache()\n+        if torch.cuda.mem_get_info()[0] >= target_mb << 20:\n+            return\n+        S = self.S0\n+        while S > self.s_floor():\n+            torch.cuda.empty_cache()\n+            free = torch.cuda.mem_get_info()[0]\n+            if free >= target_mb << 20:\n+                break\n+            S -= step\n+            try:\n+                self.set_S(S)\n+            except ArenaOOM:\n+                S += step; break\n+        logger.info(\"elastic free: S=%d (target %.2f GB, free %.2f GB)\", S, target_mb / 1024, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _regrow(self):\n+        \"\"\"After a forced shrink, grow back to the fill reserve (the free() target is transient).\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        torch.cuda.empty_cache()\n+        free = torch.cuda.mem_get_info()[0]\n+        if free < self.regrow_reserve << 20:\n+            return\n+        S = self.S0\n+        while S < self.E:\n+",
 "ttft_seconds": 13.644364378997125,
 "status": "ok",
 "tg_tokens_per_s": 30.229769459732236,
 "decode_seconds_per_token": 0.033079974405099466,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.89399299683282,
  "p95": 42.440442004590295,
  "mean": 33.07997440509946
 },
 "class": "code",
 "requested_input_tokens": 32768
}
