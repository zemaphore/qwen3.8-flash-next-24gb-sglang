{
 "started_utc": "2026-09-16T13:12:12.307816+00:00",
 "wall_seconds": 30.17685634699592,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:12:42.484822+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 108,
  "temperature_c": {
   "min": 78.0,
   "max": 81.0,
   "mean": 80.3425925925926
  },
  "power_w": {
   "min": 245.62,
   "max": 279.81,
   "mean": 265.61944444444447
  },
  "util_pct": {
   "min": 71.0,
   "max": 99.0,
   "mean": 93.6574074074074
  },
  "clock_sm_mhz": {
   "min": 1050.0,
   "max": 1905.0,
   "mean": 1724.1666666666667
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "7259ff8887df03183429795bb16987440a1ef49aa7250ef495de8db4b1c6cfd7",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        alloc+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 25+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 10+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 102+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v))+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) //+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl + \".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl + \".status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl + \".status\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl + \".status\", \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        allocates the last free byte).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] < reserve + 256 * 1024 * 1024:\n+            try:\n+                self.set_S(S + step)\n+            except Exception:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Exception:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free {free // 2**20} MB\", f\"pin_used {self.pin_used // 2**20} MB\",\n+                 f\"pin_budget {self.pin_budget // 2**20} MB\",\n+                 f\"pool {dict((n, len(v)) for n, v in self.pool.items())}\",\n+                 f\"S_floor {self.s_floor()}\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) // 2**20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB\")\n+        try:\n+            with open(self.ctl + \".status\", \"w",
 "ttft_seconds": 13.457499315998575,
 "status": "ok",
 "tg_tokens_per_s": 30.56349247470825,
 "decode_seconds_per_token": 0.03271877390411174,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.51406899653375,
  "p95": 41.24137699545827,
  "mean": 32.71877390411174
 },
 "class": "code",
 "requested_input_tokens": 32768
}
