{
 "started_utc": "2026-09-16T13:13:13.039240+00:00",
 "wall_seconds": 30.46051059099409,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:13:43.499963+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 109,
  "temperature_c": {
   "min": 80.0,
   "max": 82.0,
   "mean": 80.91743119266054
  },
  "power_w": {
   "min": 250.39,
   "max": 281.01,
   "mean": 265.44788990825685
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 94.96330275229357
  },
  "clock_sm_mhz": {
   "min": 1335.0,
   "max": 1905.0,
   "mean": 1741.1009174311926
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "f36b1b998ebec43699a08e726b132b81d3337c95d2104b9a3877cf463a6a457b",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except Arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    #+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ----------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n+    def poll+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer that\n+        cannot grow stops the loop before the reserve is breached).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E and torch.cuda.mem_get_info()[0] - self._arena_bytes() >= reserve:\n+            try:\n+                self.set_S(S + step)\n+            except ArenaOOM:\n+                break\n+            S = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        torch.cuda.empty_cache()\n+        S = max(st.S for st in self.layers.values())\n+        while S > self.s_floor() and torch.cuda.mem_get_info()[0] - self._arena_bytes() < reserve:\n+            try:\n+                self.set_S(S - step)\n+            except ArenaOOM:\n+                break\n+            S = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB\", S, torch.cuda.mem_get_info()[0] / 2**30)\n+        self.pending_regrow = True\n+\n+    def _arena_bytes(self):\n+        return sum(k.arena.n_rows * k.rb for st in self.layers.values() for k in st.kinds.values())\n+\n+    def _regrow(self):\n+        if not self.pending_regrow:\n+            return\n+        self.pending_regrow = False\n+        S = min(st.S for st in self.layers.values())\n+        if S < self.regrow_reserve:\n+            try:\n+                self.set_S(self.regrow_reserve)\n+            except ArenaOOM:\n+                pass\n+\n+    # ------------------------------------------------------------------ control\n+    def poll(self",
 "ttft_seconds": 13.524587767999037,
 "status": "ok",
 "tg_tokens_per_s": 30.17270012555087,
 "decode_seconds_per_token": 0.03314254262425719,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.63298400270287,
  "p95": 42.577767999318894,
  "mean": 33.14254262425719
 },
 "class": "code",
 "requested_input_tokens": 32768
}
