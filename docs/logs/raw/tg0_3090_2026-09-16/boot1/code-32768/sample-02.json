{
 "started_utc": "2026-09-16T13:11:41.559484+00:00",
 "wall_seconds": 30.74284173399792,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:12:12.302466+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 110,
  "temperature_c": {
   "min": 79.0,
   "max": 81.0,
   "mean": 80.07272727272728
  },
  "power_w": {
   "min": 230.04,
   "max": 282.59,
   "mean": 264.03854545454544
  },
  "util_pct": {
   "min": 86.0,
   "max": 99.0,
   "mean": 94.28181818181818
  },
  "clock_sm_mhz": {
   "min": 1290.0,
   "max": 1905.0,
   "mean": 1735.3636363636363
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "7f6422c16a1ed60e0c082f2eb3f18bed2aa774130e7291aa539c3a1e95a17de8",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\" +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                        +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized rounds).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.S0\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = min(self.E, S + step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic fill: %s\", e)\n+                break\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = self.E\n+        while True:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= reserve_mb:\n+                break\n+            S = max(self.s_floor(), S - step)\n+            try:\n+                self.set_S(S)\n+            except Exception as e:\n+                logger.warning(\"elastic free: %s\", e)\n+                break\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"free_mb={free >> 20}\", f\"total_mb={total >> 20}\", f\"S0={self.S0}\",\n+                 f\"S_floor={self.s_floor()}\", f\"pin_used_mb={self.pin_used >> 20}\",\n+                 f\"pin_budget_mb={self.pin_budget >> 20}\", \"pool=\" + \",\".join(f\"{n}:{len(v)}\" for n, v in self.pool.items()),\n+                 \"layers:\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = next(iter(st.kinds.values())).arena\n+            lines.append(f\"  L{st.lid}: S={st.S} arena={arena.n_rows}x{arena.row_bytes}B \"\n+                         f\"({arena.n_rows * arena.row_bytes / 1e6:.0f} MB)\")\n+        try:\n+            with",
 "ttft_seconds": 13.727085806000105,
 "status": "ok",
 "tg_tokens_per_s": 30.031111491007483,
 "decode_seconds_per_token": 0.03329880082192229,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 32.90342599939322,
  "p95": 41.45382199931191,
  "mean": 33.29880082192229
 },
 "class": "code",
 "requested_input_tokens": 32768
}
