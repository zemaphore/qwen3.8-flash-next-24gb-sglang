{
 "started_utc": "2026-09-16T13:11:10.927158+00:00",
 "wall_seconds": 30.621164012998634,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:11:41.548399+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 110,
  "temperature_c": {
   "min": 78.0,
   "max": 81.0,
   "mean": 79.9
  },
  "power_w": {
   "min": 244.89,
   "max": 280.45,
   "mean": 263.61027272727273
  },
  "util_pct": {
   "min": 74.0,
   "max": 99.0,
   "mean": 93.24545454545455
  },
  "clock_sm_mhz": {
   "min": 1395.0,
   "max": 1905.0,
   "mean": 1743.1363636363637
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "0559837085986a463348dbec02ae7b8bb39212284434be31439e962be66fe5c6",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-me+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[l+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows *+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.k+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >>+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w1+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_q+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, re-measured).\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S < self.E:\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = min(S + step, self.E)\n+            try:\n+                self.set_S(S)\n+            except RuntimeError:\n+                S = self.s_floor()\n+                self.set_S(S)\n+                return S\n+        return S\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        torch.cuda.empty_cache()\n+        S = min(st.S for st in self.layers.values())\n+        while S > self.s_floor():\n+            free = torch.cuda.mem_get_info()[0] >> 20\n+            if free >= int(reserve_mb):\n+                return S\n+            S = max(S - step, self.s_floor())\n+            self.set_S(S)\n+            self.pending_regrow = True\n+        return S\n+\n+    def write_status(self):\n+        if not self.ctl:\n+            return\n+        free, total = torch.cuda.mem_get_info()\n+        lines = [f\"ts={time.time():.3f} S={min(st.S for st in self.layers.values())} \"\n+                 f\"free={free >> 20}MB total={total >> 20}MB\"]\n+        for lid in sorted(self.layers):\n+            st = self.layers[lid]\n+            arena = sum(k.arena.n_rows * k.rb for k in st.kinds.values()) >> 20\n+            lines.append(f\"L{lid} S={st.S} arena={arena}MB pool={len(self.pool['w13_qweight'])}\")\n+        try:\n+            with open(self.ctl + \".status\", \"w\") as f:\n+                f.write(\"\\n\".join(lines) + \"\\n\")\n+        except OSError:\n+            pass\n+\n+    def",
 "ttft_seconds": 13.564472680001927,
 "status": "ok",
 "tg_tokens_per_s": 29.959023946019148,
 "decode_seconds_per_token": 0.03337892455381133,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.27811700000893,
  "p95": 41.57893900264753,
  "mean": 33.37892455381133
 },
 "class": "code",
 "requested_input_tokens": 32768
}
