{
 "started_utc": "2026-09-16T13:10:35.161904+00:00",
 "wall_seconds": 35.76177720900159,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:11:10.923816+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 128,
  "temperature_c": {
   "min": 75.0,
   "max": 80.0,
   "mean": 78.7265625
  },
  "power_w": {
   "min": 163.67,
   "max": 280.03,
   "mean": 247.659765625
  },
  "util_pct": {
   "min": 0.0,
   "max": 99.0,
   "mean": 77.984375
  },
  "clock_sm_mhz": {
   "min": 960.0,
   "max": 1905.0,
   "mean": 1758.515625
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1350.0
  }
 },
 "output_text_sha256": "6d43da8d3932b181fdbaea418e6df617482ee350bf2a47bc3963b976a2fdc153",
 "output_text": "++       +        \"\"\"+        \"\"\"Grow+        \"\"\"Grow every+        \"\"\"Grow every layer+        \"\"\"Grow every layer until+        \"\"\"Grow every layer until `+        \"\"\"Grow every layer until `reserve+        \"\"\"Grow every layer until `reserve_mb+        \"\"\"Grow every layer until `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur ++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Sh+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb`+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(res+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur >+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+               +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\",+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] /+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**3+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30,+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time()+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() -+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >=+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow =+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+   +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VR+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+       +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0]+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] <+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.reg+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve <<+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 2+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n++        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+           +        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return+        \"\"\"Grow every layer until `reserve_mb` of VRAM is free (step-sized, so a layer never\n+        grabs the last bytes the reserve needs).\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = min(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] > reserve and cur < self.E:\n+            target = min(cur + step, self.E)\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = min(st.S for st in self.layers.values())\n+        logger.info(\"elastic fill: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+\n+    def free(self, reserve_mb, step=8):\n+        \"\"\"Shrink every layer until at least `reserve_mb` of VRAM is free.\"\"\"\n+        reserve = int(reserve_mb) << 20\n+        t0 = time.time()\n+        torch.cuda.empty_cache()\n+        cur = max(st.S for st in self.layers.values())\n+        while torch.cuda.mem_get_info()[0] < reserve and cur > self.s_floor():\n+            target = max(cur - step, self.s_floor())\n+            try:\n+                self.set_S(target)\n+            except RuntimeError:\n+                break\n+            cur = max(st.S for st in self.layers.values())\n+        logger.info(\"elastic free: S=%d, VRAM free %.2f GB, %.2fs\", cur, torch.cuda.mem_get_info()[0] / 2**30, time.time() - t0)\n+        if self.regrow_reserve and torch.cuda.mem_get_info()[0] >= self.regrow_reserve << 20:\n+            self.pending_regrow = True\n+\n+    def regrow(self):\n+        \"\"\"Grow back after a forced shrink once the VRAM is available again.\"\"\"\n+        if not self.pending_regrow:\n+            return\n+        if torch.cuda.mem_get_info()[0] < self.regrow_reserve << 20:\n+            return\n",
 "ttft_seconds": 19.264827717001026,
 "status": "ok",
 "tg_tokens_per_s": 30.975538918797362,
 "decode_seconds_per_token": 0.03228353839529664,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 31.71515800204361,
  "p95": 41.39235599723179,
  "mean": 32.283538395296645
 },
 "class": "code",
 "requested_input_tokens": 32768
}
