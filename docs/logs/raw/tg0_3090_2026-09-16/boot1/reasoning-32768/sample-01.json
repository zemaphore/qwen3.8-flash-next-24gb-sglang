{
 "started_utc": "2026-09-16T13:17:17.428463+00:00",
 "wall_seconds": 26.85492362199875,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:17:44.283540+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 96,
  "temperature_c": {
   "min": 79.0,
   "max": 81.0,
   "mean": 80.4375
  },
  "power_w": {
   "min": 263.38,
   "max": 282.13,
   "mean": 273.4372916666667
  },
  "util_pct": {
   "min": 86.0,
   "max": 99.0,
   "mean": 92.89583333333333
  },
  "clock_sm_mhz": {
   "min": 1110.0,
   "max": 1905.0,
   "mean": 1714.21875
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "87c16c35cfd9373006d02d2d93f89513df806188c478d6c54b868634239ba06f",
 "output_text": " last last  last 1 last 16 last 16 tokens last 16 tokens of last 16 tokens of every last 16 tokens of every request last 16 tokens of every request, last 16 tokens of every request, so last 16 tokens of every request, so f last 16 tokens of every request, so f >= last 16 tokens of every request, so f >=  last 16 tokens of every request, so f >= 0 last 16 tokens of every request, so f >= 0. last 16 tokens of every request, so f >= 0.0 last 16 tokens of every request, so f >= 0.06 last 16 tokens of every request, so f >= 0.06.\", last 16 tokens of every request, so f >= 0.06.\",\n last 16 tokens of every request, so f >= 0.06.\",\n  last 16 tokens of every request, so f >= 0.06.\",\n  \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \" last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/s last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale ->  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 ->  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = abs last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (f last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_q last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_r last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rms last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_ro last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-11 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-R last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-e last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sig last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-opt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multip last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) /  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. N last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: d last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dN last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (d last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlog last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With log last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (me last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), d last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlog last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.06 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) /  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.2 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> d last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dN last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.000 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 /  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.00 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004- last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0. last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.0 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 n last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold gran last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules ( last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (un last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unme last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last  last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 1 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by the indexer last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by the indexer in last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by the indexer in the last 16 tokens of every request, so f >= 0.06.\",\n  \"expected_accuracy\": \"Error model (uniform rounding noise, RMS = step/sqrt(12)): hot K/V = e4m3 unit scale -> 2.7 % relative RMS (measured on N(0,3)); cold K/V int4-g32 -> 8.6 % (step = absmax/7 over 32 channels). The cold K is smoothed by the k_norm weight (1+w_c), which is exactly the transform the model already applies to K before storing it (fused_qk_rmsnorm_rope_gate.py:82-118), so after dividing by 1+w_c the stored rows are unit-RMS with near-equal channel sigmas (gamma spread 1.25-1.48 measured on the fp8 run) -> the 32-channel group scale is near-optimal and the 8.6 % is the floor for 4 bits. The indexer keys stay bf16 (exact). Attention output error: softmax weights carry multiplicative noise ~2.7 % (hot) / 8.6 % (cold) x sqrt(sum p_i^2) ~ 0.3 for peaked attention -> ~0.8 % / ~2.6 % RMS on the output; V adds ~0.8 % / ~2.6 %. NLL impact is second order in noise: dNLL ~ (1/2) * E[sum p_i (dlogit_i)^2] * (1 - sum p_i^2). With logit std ~2.4 nats (measured in the simulation), dlogit ~ 0.065 nats (hot) / 0.21 nats (cold) -> dNLL ~ 0.0003 / 0.001 nats per layer, summed over 12 layers ~ 0.004-0.04 nats/token, bounded by the fraction of attention mass that lives in cold granules (unmeasured; the design's own experiment measures it). The hot tier guarantees that the newest tokens, the current chunk, the sink and the last 16 tokens of every request are never degraded, so the first-token and the newest-token quality is exact; the cold tier only affects tokens that have not been selected by the indexer in the last",
 "ttft_seconds": 13.231337175995577,
 "status": "ok",
 "tg_tokens_per_s": 37.50869871222725,
 "decode_seconds_per_token": 0.026660482350298532,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 25.974465999752283,
  "p95": 33.67219399660826,
  "mean": 26.660482350298533
 },
 "class": "reasoning",
 "requested_input_tokens": 32768
}
