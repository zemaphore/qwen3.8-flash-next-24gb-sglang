{
 "started_utc": "2026-09-16T13:06:10.743527+00:00",
 "wall_seconds": 14.518885842997406,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:06:25.262583+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 71.0,
   "max": 74.0,
   "mean": 73.25
  },
  "power_w": {
   "min": 258.61,
   "max": 275.58,
   "mean": 266.53653846153844
  },
  "util_pct": {
   "min": 86.0,
   "max": 100.0,
   "mean": 89.61538461538461
  },
  "clock_sm_mhz": {
   "min": 1635.0,
   "max": 1920.0,
   "mean": 1908.75
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "5e9213e6a896e0c9d37038bd1a60ee24b807bb12308423be823f13dd5c2c8185",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2621.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26214.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-comp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, M.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench21.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench21.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench21.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-tier int4` (the default tiered cache), `--ple`\nwith the PLE table on NVMe, `--mem-fraction-static 0.85` and `--cuda-fla`. The `PLE` variable must point at\nthe directory holding `ple.json` and `ple.f8_e4m3.bin` (the Hub layout puts them under `ple/`). The\n`--ple` flag and the `--kv-tier` flag are part of the patch.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the patch scripts, by layer: base, quant, kv_tiers, elastic, ple, fla, mtp, bench, serve\nkernels/        the Triton kernels the patch JIT-compiles (int2 MoE, int8 ring, int4 KV, PLE, MTP)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), bench.sh, mtp.sh, mtpbench.sh,\n              mtpbench2.sh, mtpbench3.sh, mtpbench4.sh, mtpbench5.sh, mtpbench6.sh, mtpbench7.sh,\n              mtpbench8.sh, mtpbench9.sh, mtpbench10.sh, mtpbench11.sh, mtpbench12.sh, mtpbench13.sh,\n              mtpbench14.sh, mtpbench15.sh, mtpbench16.sh, mtpbench17.sh, mtpbench18.sh, mtpbench19.sh,\n              mtpbench20.sh, mtpbench21.sh, m",
 "ttft_seconds": 1.0813365959984367,
 "status": "ok",
 "tg_tokens_per_s": 38.02799969007785,
 "decode_seconds_per_token": 0.026296413383555303,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.26274099748116,
  "p95": 31.78793500410393,
  "mean": 26.296413383555304
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
