{
 "started_utc": "2026-09-16T13:07:10.726608+00:00",
 "wall_seconds": 15.341548901000351,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:07:26.068223+00:00",
 "warmup": false,
 "sample_index": 5,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 55,
  "temperature_c": {
   "min": 76.0,
   "max": 78.0,
   "mean": 76.63636363636364
  },
  "power_w": {
   "min": 251.23,
   "max": 276.99,
   "mean": 264.28527272727274
  },
  "util_pct": {
   "min": 83.0,
   "max": 100.0,
   "mean": 89.85454545454546
  },
  "clock_sm_mhz": {
   "min": 1635.0,
   "max": 1920.0,
   "mean": 1901.1818181818182
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "38bc06ca14675dd1ddfa4909ea3068d6bcf63d15f7285f323686f180628b3b32",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=....py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills);.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `k.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n|.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 40.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 409.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 20.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 204.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 76.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 40.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 409.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide M.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture is described.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture is described in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture is described in [`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 101\npython3 tools/ple.py --model ~/quant/model --ple ~/quant/model/ple --prompt-tokens 101 --max-tokens 101\n```\n\n`scripts/serve.sh` prints `launch: pid=...` and the server answers `GET /health` within about 100 seconds.\nThe first request takes roughly 100 seconds more (the PLE table loads into host RAM and the expert arena\nfills); after that, decode runs at the headline rate. The server's own log records the arena fill and the\nPLE load; `tools/ple.py` prints the PLE load time and the first-request decode rate.\n\n## What is in this repository\n\n| Path | Contents |\n|---|---|\n| `sglang/` | the serving patch (one `git apply` patch, 10 files, 1,055 added lines) and the layered patch scripts it was built from |\n| `patches/` | the patch scripts for the other components: the PLE loader, the KV cache tiers, the elastic expert cache |\n| `kernels/` | the Triton kernels: the expert GEMM, the PLE embedding, the INT4 KV page |\n| `assets/` | the two serving assets: the accepted state and the expert arena layout |\n| `tools/` | the measurement tools: the benchmark, the PLE probe, the KV cache probe, the arena probe |\n| `docs/` | the design documents, the engineering log, the measurement logs |\n| `scripts/` | the launch script |\n| `tests/` | the unit tests |\n\n## The serving stack\n\nThe model is a Qwen4-Exp: 48 layers, 40 attention heads of 128 dimensions, 8 KV heads, GQA, a 4096-wide\nhidden state, a 2048-wide shared expert, 48 routed experts of width 768, 8 experts routed per token, a\n256k-token context, a 4096-wide MTP layer, and a PLE (per-layer embedding) block on every layer. The\narchitecture is described in [`docs",
 "ttft_seconds": 1.1723209199990379,
 "status": "ok",
 "tg_tokens_per_s": 36.064195821801825,
 "decode_seconds_per_token": 0.027728332136979793,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.15226799773518,
  "p95": 35.828624000714626,
  "mean": 27.728332136979795
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
