{
 "started_utc": "2026-09-16T13:06:25.264424+00:00",
 "wall_seconds": 14.824782618001336,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:06:40.089280+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 53,
  "temperature_c": {
   "min": 74.0,
   "max": 75.0,
   "mean": 74.45283018867924
  },
  "power_w": {
   "min": 254.02,
   "max": 277.92,
   "mean": 265.55094339622644
  },
  "util_pct": {
   "min": 89.0,
   "max": 100.0,
   "mean": 91.0
  },
  "clock_sm_mhz": {
   "min": 1665.0,
   "max": 1920.0,
   "mean": 1905.0
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "c20a819f83fa55bf5bddf515cdb758ac339fd46d36a1701884fa3df12f5d3f1d",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 2621.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 26214.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-d.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-g.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-c.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n102.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` sub.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gem.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/      .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4-Exp model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4-Exp model classes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang and waits for `/health`. The published configuration is `--max-running-requests 1` (one request at a\ntime), `--max-total-tokens 262144` (full context), `--kv-cache-dtype int8ring_int4` (the default tiered KV\nmode), `--ple-ple-external` (the PLE table on NVMe), `--ple-ple-gpu-cache-size 128` and `--ple-ple-cpu-cache-size\n1024`. The `PLE` variable must point at the directory holding `ple.json` and `ple.f8_e4m3.bin` (the\n`ple/` subdirectory of the checkpoint directory). The server's own log is `logs/serve.log` in the repository\ndirectory.\n\n## Repository layout\n\n```\nsglang/        the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       the patch scripts, the serving patch, the Triton kernels, the KV-cache tier module\nkernels/       the CUDA kernels (int2 gemv, int4 gemv, int8 gemv, int4 gemm, int8 gemm, int8 ring)\nscripts/       the launcher (serve.sh), the build scripts, the CUDA kernel build\ntools/         the measurement tools (bench.py, ngram.py, ngram_nll.py, ngram_nll_bf16.py, ngram_nll_int4.py)\ndocs/          CAMPAIGN.md (the engineering log), the design documents, the measurement logs\nassets/        the accepted state (phase1_state.json), the PLE table (ple.json), the CUDA kernel object\nLICENSE        Apache-2.0 (code); the weights are under the Qwen Community License 1.0 on the Hub\n```\n\n## What is in the patch\n\nThe patch is the serving code: the Qwen4-Exp model classes (",
 "ttft_seconds": 1.0550156000026618,
 "status": "ok",
 "tg_tokens_per_s": 37.110411849860306,
 "decode_seconds_per_token": 0.02694661552250502,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.574854004138615,
  "p95": 33.865625999169424,
  "mean": 26.94661552250502
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
