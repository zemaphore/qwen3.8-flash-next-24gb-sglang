{
 "started_utc": "2026-09-16T13:06:40.090964+00:00",
 "wall_seconds": 15.100600764002593,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:06:55.191648+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 54,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.29629629629629
  },
  "power_w": {
   "min": 256.9,
   "max": 276.04,
   "mean": 266.0379629629629
  },
  "util_pct": {
   "min": 88.0,
   "max": 100.0,
   "mean": 90.83333333333333
  },
  "clock_sm_mhz": {
   "min": 1665.0,
   "max": 1920.0,
   "mean": 1901.111111111111
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "09c9a5c5b8352c6a9219598c658fc658c5bdb4d54c467ef8509948b4460fd284",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) +.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, N.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VR.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 delet.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 Mo.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nT.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTrit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from, in build\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from, in build\norder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers; it sets the environment, launches\nSGLang with the patch's flags and waits for `/health`. The published decode numbers were measured with\n`--max-running-requests 1`; the launcher's default is 4 (CAMPAIGN.md:413). The full flag set is in\n`assets/phase1_state.json` (`accepted.env`); the accepted state is the flag set, environment, patch list\nand checkpoint recorded in `assets/phase1_state.json`, on which the published numbers were measured.\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang 73a255206f) + the patch list\npatches/        the layered patch scripts the serving patch was built from (10 files)\nkernels/        the Triton kernels (int2 MoE, int8 ring, int4 KV, PLE, int8 GEMM) + the sm_120 configs\nassets/         the two serving assets: the PLE n-gram table and the phase-1 state record\ntools/          the measurement tools (bench, NLL probe, KV probe, PLE probe, VRAM probe, ...)\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is a single `git apply` patch against SGLang at commit\n`73a255206f` (1,050 insertions, 10 deletions, 15 files). It adds the Qwen4-Exp model class, the int2 MoE\npath, the tiered KV cache, the PLE path, the elastic expert cache, the int8 GEMM path and the sm_120\nTriton configs. `sglang/patch_list.txt` records the 10 layered patch scripts it was built from, in build\norder,",
 "ttft_seconds": 1.1233244700051728,
 "status": "ok",
 "tg_tokens_per_s": 36.55948420540371,
 "decode_seconds_per_token": 0.02735268348923243,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.904665006441064,
  "p95": 33.629261000896804,
  "mean": 27.352683489232433
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
