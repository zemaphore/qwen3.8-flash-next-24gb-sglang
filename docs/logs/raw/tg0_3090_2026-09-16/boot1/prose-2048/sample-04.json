{
 "started_utc": "2026-09-16T13:06:55.193354+00:00",
 "wall_seconds": 15.53134014600073,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:07:10.724801+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 55,
  "temperature_c": {
   "min": 75.0,
   "max": 77.0,
   "mean": 76.03636363636363
  },
  "power_w": {
   "min": 255.32,
   "max": 276.5,
   "mean": 263.8978181818182
  },
  "util_pct": {
   "min": 87.0,
   "max": 100.0,
   "mean": 90.8
  },
  "clock_sm_mhz": {
   "min": 1545.0,
   "max": 1920.0,
   "mean": 1896.8181818181818
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2642.0
  }
 },
 "output_text_sha256": "146b50b3ebef0ae849ff4c499e8d2880cb6be3b0bb2ce4b3538dcd9c98ec6e04",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (git.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored)..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,05.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :11.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :123.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :123,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :123, :.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :123, :1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple_probe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch, so `PLE` must point at `ple/` on the NVMe. The server's own log is written to\n`docs/logs/serve.log` (gitignored).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one file)\npatches/        the layered patch scripts the patch was built from\nkernels/        the Triton kernels, the CUDA PLE kernel, the sm_120a Triton PTX\nassets/         the two serving assets (the PLE table, the expert arena)\ntools/          the measurement tools\ndocs/           the design documents, the engineering log, the measurement logs\nscripts/        the launcher and the build scripts\n```\n\n## The serving patch\n\n`sglang/qwen4exp-serving-73a255206f.patch` is the single file that carries the serving patch: 15 files,\n1,050 added lines, 104 removed, against SGLang at `73a255206f`. It is the final state of the layered patch\nscripts in `patches/` (CAMPAIGN.md:100, :104, :106, :107, :110, :111, :112, :113, :114, :115, :116, :117,\n:118, :119, :120, :121, :122, :123, :12",
 "ttft_seconds": 1.1695933920054813,
 "status": "ok",
 "tg_tokens_per_s": 35.58078841647956,
 "decode_seconds_per_token": 0.028105054567504784,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.925337002670858,
  "p95": 35.51640099612996,
  "mean": 28.105054567504784
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
