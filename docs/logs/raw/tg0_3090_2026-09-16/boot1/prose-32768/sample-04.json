{
 "started_utc": "2026-09-16T13:15:44.479547+00:00",
 "wall_seconds": 28.037859666001168,
 "prompt_tokens": 32768,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-16T13:16:12.517555+00:00",
 "warmup": false,
 "sample_index": 4,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 100,
  "temperature_c": {
   "min": 80.0,
   "max": 82.0,
   "mean": 81.42
  },
  "power_w": {
   "min": 232.37,
   "max": 282.05,
   "mean": 270.9435
  },
  "util_pct": {
   "min": 28.0,
   "max": 99.0,
   "mean": 89.62
  },
  "clock_sm_mhz": {
   "min": 1065.0,
   "max": 1905.0,
   "mean": 1702.35
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 1474.0
  }
 },
 "output_text_sha256": "c099fb5a79b0001ae62e4107be3ce51796eb9ae4c3316f627e2466a4d0c96ce4",
 "output_text": "okensokens okens 1okens 13okens 131okens 1310okens 13107okens 131072okens 131072 becomesokens 131072 becomes possibleokens 131072 becomes possible onokens 131072 becomes possible on aokens 131072 becomes possible on a okens 131072 becomes possible on a 2okens 131072 becomes possible on a 24okens 131072 becomes possible on a 24 GBokens 131072 becomes possible on a 24 GB cardokens 131072 becomes possible on a 24 GB card;okens 131072 becomes possible on a 24 GB card; theokens 131072 becomes possible on a 24 GB card; the profileokens 131072 becomes possible on a 24 GB card; the profiledokens 131072 becomes possible on a 24 GB card; the profiled valueokens 131072 becomes possible on a 24 GB card; the profiled value staysokens 131072 becomes possible on a 24 GB card; the profiled value stays theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physicalokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceilingokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Whyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why itokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it shouldokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should payokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131kokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KVokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV poolokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131kokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k xokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB =okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB ofokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed byokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INTokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 denseokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, sookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expertokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cacheokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot growokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131kokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k;okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; withokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backingokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the poolokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool isokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is addressokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address spaceokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space andokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backingokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing followsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows liveokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slotsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, sookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so bothokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both canokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can existokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untestedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocatorokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator resetokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset onokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idleokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (nookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hookokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook existsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists;okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the releaseokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs onokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the nextokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next allocokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extendokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to startokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start orokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or benchokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> revertedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attemptokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED atokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startupokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic pathokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pinsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 18okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows perokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layerokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB)okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) onokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on topokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top ofokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloaderokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader'sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host Ookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOMokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. Theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cacheokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache mustokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACEokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placementokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement'sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, notokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not addokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to themokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows areokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are alreadyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arenaokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, sookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so theyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they areokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are neverokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned;okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPUokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' coldokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows getokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get poolokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slotsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacateokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate themokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them).okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floorokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 18okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs nookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallbackokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budgetokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to startokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start orokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or benchokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> revertedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attemptokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED atokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startupokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic pathokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pinsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 18okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows perokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layerokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB)okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) onokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on topokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top ofokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloaderokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader'sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GBokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host Ookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOMokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. Theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cacheokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache mustokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACEokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE theokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placementokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement'sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, notokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not addokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to themokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows areokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are alreadyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arenaokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows,okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, sookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so theyokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they areokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are neverokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned;okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPUokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' coldokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows getokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get poolokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slotsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (hostokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layersokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers'okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hotokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rowsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacateokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate themokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them).okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floorokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 18okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needsokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs nookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinnedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallbackokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budgetokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to startokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start orokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or benchokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> revertedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-0okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 Sokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S1okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elasticokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic:okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILEDokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED tookens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to startokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start orokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or benchokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench ->okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> revertedokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\nokens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n-okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- 2okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- 20okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- 202okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- 2026okens 131072 becomes possible on a 24 GB card; the profiled value stays the physical ceiling. Why it should pay: the 131k KV pool costs 131k x 24 KB = 3.2 GB of the 3.5 GB freed by INT8 dense, so the expert cache cannot grow to 131k; with lazy backing the pool is address space and the backing follows live slots, so both can exist. Untested: allocator reset on idle (no hook exists; the release runs on the next alloc_extend).\n- 2026-09-02 13:06 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:08 S13_elastic attempt 2 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:11 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:14 S13_elastic attempt 3 FAILED at startup: the elastic path pins 184 hot rows per layer (24 GB) on top of the offloader's 24 GB -> host OOM. The elastic cache must REPLACE the placement's pinned rows, not add to them: host layers' hot rows are already arena rows, so they are never pinned; GPU layers' cold rows get pool slots (host layers' hot rows vacate them). S_floor 184 needs no pinned fallback budget.\n- 2026-09-02 13:17 S13_elastic: FAILED to start or bench -> reverted\n- 2026-09-02 13:20 S13_elastic: FAILED to start or bench -> reverted\n- 2026-",
 "ttft_seconds": 13.761914893999347,
 "status": "ok",
 "tg_tokens_per_s": 35.79467407529206,
 "decode_seconds_per_token": 0.027937117066537804,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.417391997005325,
  "p95": 35.46170400659321,
  "mean": 27.937117066537805
 },
 "class": "prose",
 "requested_input_tokens": 32768
}
