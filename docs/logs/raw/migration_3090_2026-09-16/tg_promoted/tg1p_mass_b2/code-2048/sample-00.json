{
 "started_utc": "2026-09-17T04:45:49.322074+00:00",
 "wall_seconds": 18.940465782987303,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:46:08.262726+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 68,
  "temperature_c": {
   "min": 74.0,
   "max": 76.0,
   "mean": 75.02941176470588
  },
  "power_w": {
   "min": 216.94,
   "max": 273.93,
   "mean": 245.47897058823528
  },
  "util_pct": {
   "min": 44.0,
   "max": 97.0,
   "mean": 89.27941176470588
  },
  "clock_sm_mhz": {
   "min": 1680.0,
   "max": 1905.0,
   "mean": 1892.8676470588234
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2380.0
  }
 },
 "output_text_sha256": "7503c683be997f3c48a4bf4cc85fdfcf5f667192409dcd0e192e7edbe2294239",
 "output_text": "++       +        b+        b_z+        b_zp+        b_zp_num+        b_zp_num =+        b_zp_num = +        b_zp_num = 1+        b_zp_num = 12+        b_zp_num = 128+        b_zp_num = 128\n+        b_zp_num = 128\n++        b_zp_num = 128\n+   +        b_zp_num = 128\n+    elif+        b_zp_num = 128\n+    elif has+        b_zp_num = 128\n+    elif has_z+        b_zp_num = 128\n+    elif has_zp+        b_zp_num = 128\n+    elif has_zp and+        b_zp_num = 128\n+    elif has_zp and use+        b_zp_num = 128\n+    elif has_zp and use_int+        b_zp_num = 128\n+    elif has_zp and use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_z+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_exp+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 6+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off ++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K *+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+   +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the var+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the var+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three var+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k`+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three var+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        #+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+       +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:,+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None]+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] <+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 -+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 //+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 <<+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_sh+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter))+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) &+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n++        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+           +        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+            b+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+            b0+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+            b0 =+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+            b0 = tl+        b_zp_num = 128\n+    elif has_zp and use_int2_w2a16:\n+        b_zp_shifter = (tl.arange(0, 16) * 2)[None, :, None]\n+\n+    if use_int8_w8a16:\n+        b_scale_src_off = off_experts * stride_bse\n+        b_scale_idx = offs_bn * (stride_bsn // 64)\n+        b_scale_ptrs = (\n+            b_scale_ptr + b_scale_src_off + offs_k[:, None] * stride_bsk\n+        )\n+        b_scale_repeat = BLOCK_SIZE_K * BLOCK_SIZE_N // group_size\n+    else:\n+        b_scale_repeat = 0\n+\n+    for k in range(0, K, BLOCK_SIZE_K):\n+        # --------------------------------------------\n+        # Loop across K dimension\n+        # Note: our computation might be affected by the variances and the\n+        # strides of A, when the tokens are represented as the\n+        # `[BLOCK_SIZE_M, BLOCK_SIZE_K]` shape, we need to skip the variances\n+        # and the strides, since they are not in the shape of the 2D tensor.\n+        # We don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        # Note: we don't always expect to be indexed elements based on the fact that\n+        # we take remainder of `top_k` later, we pass index\u63a9\u7801 two params to index_any01\n+        # to deal w/ three variances stride mixed.\n+        # --------------------------------------------\n+        a = tl.load(a_ptrs, mask=token_mask[:, None] & (offs_k[None, :] < K - k))\n+        if use_int2_w2a16:\n+            b0 = tl.load(b_ptrs, mask=offs_kw[:, None] < K // 16 - k // 16)\n+            b0 = (b0 // (1 << b_shifter)) & 0x3\n+            b0 = tl.reshape",
 "ttft_seconds": 1.5694877459900454,
 "status": "ok",
 "tg_tokens_per_s": 29.41700079995254,
 "decode_seconds_per_token": 0.03399394815264829,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 33.77636399818584,
  "p95": 43.006325999158435,
  "mean": 33.993948152648294
 },
 "class": "code",
 "requested_input_tokens": 2048
}
