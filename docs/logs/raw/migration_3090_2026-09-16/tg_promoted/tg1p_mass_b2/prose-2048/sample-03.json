{
 "started_utc": "2026-09-17T04:44:33.676313+00:00",
 "wall_seconds": 13.729698782000924,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:44:47.406110+00:00",
 "warmup": false,
 "sample_index": 3,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 49,
  "temperature_c": {
   "min": 73.0,
   "max": 75.0,
   "mean": 74.44897959183673
  },
  "power_w": {
   "min": 250.83,
   "max": 273.84,
   "mean": 266.82020408163265
  },
  "util_pct": {
   "min": 82.0,
   "max": 93.0,
   "mean": 88.3061224489796
  },
  "clock_sm_mhz": {
   "min": 1905.0,
   "max": 1920.0,
   "mean": 1918.469387755102
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "b145d4b11a33b58fe5588965e307e0880e8645e975d2ad33e562b4b038a6bdaa",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:300.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nS.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/b.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and pre.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthrough.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py`.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag;.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [tr.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troub.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#tr.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troub.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-m.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tier.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-comp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh).py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here..py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Q.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer:.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters),.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,0.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,00.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary,.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary, .py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary, 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary, 25.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\npython3 tools/ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers: it sets the environment, launches\nSGLang, waits for `/health` and prints the accepted state. `tools/bench.py` measures decode and prefill\nthroughput; `tools/ple.py` checks the PLE n-gram table against the reference implementation. The\n[environment](#environment) section explains every flag; the [troubleshooting](#troubleshooting) section\nlists the launch failures and their fixes.\n\n## What is in this repository\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's layers: the Qwen4-Exp model, the int2-mixed loader, the PLE table,\n              the tiered KV cache, the expert cache, the elastic controller, the Triton configs\nkernels/        the Triton kernels the patch JIT-compiles (int2 expert GEMM, INT8 KV ring, INT4 KV pool,\n              PLE gather, PLE n-gram lookup, PLE output projection)\nassets/         the two serving assets: the accepted state and the PLE n-gram table\ntools/          the measurement tools: bench.py, ple.py, ngram.py, ngram_ref.py, ngram_quality.py,\n              ngram_quality_ref.py, ngram_quality_run.py, ngram_quality_run_ref.py\ndocs/         the design documents with their outcomes and the dated engineering log\nscripts/        the launcher (serve.sh) and the build scripts\n```\n\nThe repository is code and documentation only: the weights are on the Hub, not here.\n\n## How it works\n\nThe model is a Qwen4-Exp mixture-of-experts transformer: 48 layers, 128 experts per layer, 8 experts\nactivated per token (6B active parameters), a 128,000-entry vocabulary, 256",
 "ttft_seconds": 0.3079559750040062,
 "status": "ok",
 "tg_tokens_per_s": 38.07274134063843,
 "decode_seconds_per_token": 0.026265510829728746,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.032259993371554,
  "p95": 32.26597899629269,
  "mean": 26.265510829728747
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
