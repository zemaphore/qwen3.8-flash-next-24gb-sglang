{
 "started_utc": "2026-09-17T04:43:46.215713+00:00",
 "wall_seconds": 16.867977955000242,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:44:03.083856+00:00",
 "warmup": true,
 "sample_index": 0,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 61,
  "temperature_c": {
   "min": 64.0,
   "max": 72.0,
   "mean": 69.88524590163935
  },
  "power_w": {
   "min": 128.4,
   "max": 268.49,
   "mean": 242.31475409836068
  },
  "util_pct": {
   "min": 0.0,
   "max": 96.0,
   "mean": 80.63934426229508
  },
  "clock_sm_mhz": {
   "min": 1740.0,
   "max": 1935.0,
   "mean": 1918.27868852459
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "a4c4c48c9c719d1647c42e897b0d120b66d1e586f075b2d84c8c6bc9868554fc",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:32.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01...py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (through.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/          .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/     .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/    .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/ab.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/ .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/defer.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/  .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/202.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/202.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/202.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-0.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes/2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes/20.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes/202.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--max-running-requests 2` is the elastic mode (CAMPAIGN.md:320). The PLE table lives on\nNVMe and is read at launch; `PLE` must point at `ple/` (the directory, not the file).\n\n## Repository layout\n\n```\nsglang/         the serving patch (one git-apply patch against SGLang at 73a255206f)\npatches/        the layered patch scripts the patch was built from (01..06, in build order)\nkernels/        the Triton kernels: int2 expert GEMM, int8 ring KV, int4 KV, PLE, routing\ntools/          bench.py (throughput), ngram_ple.py (PLE table builder), ngram_ple_test.py (its tests)\nscripts/        serve.sh (the launcher), build_ple.sh (the PLE builder), build_int2.sh (the quantizer)\nassets/         phase1_state.json (the accepted state), ple.json (the PLE table's metadata)\ndocs/           CAMPAIGN.md (the engineering log), the design documents, the measurement logs\ndocs/logs/      the dated measurement logs (night.log, tiers-validate.log, elastic.ctl.status, ...)\ndocs/notes/     the design documents with their outcomes\ndocs/notes/abandoned/  the designs that were dropped, with the reasons\ndocs/notes/deferred/   the designs deferred to a later phase\ndocs/notes/2026-06-22/ the design documents of the first phase\ndocs/notes/2026-06-23/ the design documents of the second phase\ndocs/notes/2026-06-24/ the design documents of the third phase\ndocs/notes/2026",
 "ttft_seconds": 3.008183026991901,
 "status": "ok",
 "tg_tokens_per_s": 36.869469267533376,
 "decode_seconds_per_token": 0.027122712093948773,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 26.064001009217463,
  "p95": 35.46014199673664,
  "mean": 27.122712093948774
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
