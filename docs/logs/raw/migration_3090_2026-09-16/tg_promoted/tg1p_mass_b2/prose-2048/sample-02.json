{
 "started_utc": "2026-09-17T04:44:19.120747+00:00",
 "wall_seconds": 14.553170427010627,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:44:33.674045+00:00",
 "warmup": false,
 "sample_index": 2,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 52,
  "temperature_c": {
   "min": 71.0,
   "max": 74.0,
   "mean": 73.42307692307692
  },
  "power_w": {
   "min": 229.65,
   "max": 270.78,
   "mean": 259.9317307692308
  },
  "util_pct": {
   "min": 87.0,
   "max": 93.0,
   "mean": 90.40384615384616
  },
  "clock_sm_mhz": {
   "min": 1890.0,
   "max": 1920.0,
   "mean": 1917.6923076923076
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2428.0
  }
 },
 "output_text_sha256": "e5ce255e7b6dfd1106bdbaa8d872db4d047e8a23c8f119a1172a149676a199cc",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`;.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 26.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,14.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_b.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n             .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py, ngram_v.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py, ngram_v3.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published configuration is `--max-running-requests 1`\nwith `--enable-ple`; `--enable-ple` is the only way to reach the full 262,144-token context on 24 GB, and it\nis what the headline decode numbers were measured with. The `PLE` variable must point at `ple/` (the table\nplus `ple.json`), not at the model root.\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the 13 patches that make up the serving patch, in build order\nkernels/        the Triton kernels: int2_moe.py (the expert path), ple.py (the PLE path)\nscripts/        serve.sh (the launcher), build.sh (the patch builder), plus the build and measurement scripts\ntools/        the measurement tools: bench.py, ngram.py, ngram_bench.py, ngram_quality.py, ngram_v3.py,\n              ngram_v3_bench.py, ngram_v3_quality.py, ngram_v3_258k.py, ngram_v3_258k_bench.py,\n              ngram_v3_258k_quality.py, ngram_v3_258k_258k.py, ngram_v3_258k_258k_bench.py,\n              ngram_v3_258k_258k_quality.py, ngram_v3_258k_258k_258k.py, ngram_v3_258k_258k_258k_bench.py,\n              ngram_v3_258k_258k_258k_quality.py, ngram_v3_",
 "ttft_seconds": 0.49031231000844855,
 "status": "ok",
 "tg_tokens_per_s": 36.33705873682169,
 "decode_seconds_per_token": 0.027520114031317097,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.1402689977549,
  "p95": 35.137478000251576,
  "mean": 27.520114031317096
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
