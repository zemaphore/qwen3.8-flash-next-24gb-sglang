{
 "started_utc": "2026-09-17T04:44:03.086099+00:00",
 "wall_seconds": 16.032228223004495,
 "prompt_tokens": 2048,
 "completion_tokens": 512,
 "stream_events": 512,
 "ended_utc": "2026-09-17T04:44:19.118661+00:00",
 "warmup": false,
 "sample_index": 1,
 "requested_decode_tokens": 512,
 "gpu": {
  "n": 58,
  "temperature_c": {
   "min": 71.0,
   "max": 73.0,
   "mean": 72.48275862068965
  },
  "power_w": {
   "min": 243.41,
   "max": 267.6,
   "mean": 259.8113793103448
  },
  "util_pct": {
   "min": 85.0,
   "max": 98.0,
   "mean": 89.82758620689656
  },
  "clock_sm_mhz": {
   "min": 1800.0,
   "max": 1920.0,
   "mean": 1912.7586206896551
  },
  "clock_mem_mhz": {
   "min": 9501.0,
   "max": 9501.0,
   "mean": 9501.0
  },
  "memory_free_mib": {
   "min": 2540.0
  }
 },
 "output_text_sha256": "97bf36c3f45f9df7007dd8c853458c0f9237cd78cf79788e9555792a339584e0",
 "output_text": ".py.py --.py --url.py --url http.py --url http://.py --url http://1.py --url http://12.py --url http://127.py --url http://127..py --url http://127.0.py --url http://127.0..py --url http://127.0.0.py --url http://127.0.0..py --url http://127.0.0.1.py --url http://127.0.0.1:.py --url http://127.0.0.1:3.py --url http://127.0.0.1:30.py --url http://127.0.0.1:300.py --url http://127.0.0.1:3000.py --url http://127.0.0.1:30000.py --url http://127.0.0.1:30000 --.py --url http://127.0.0.1:30000 --prompt.py --url http://127.0.0.1:30000 --prompt-t.py --url http://127.0.0.1:30000 --prompt-tokens.py --url http://127.0.0.1:30000 --prompt-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 10.py --url http://127.0.0.1:30000 --prompt-tokens 101.py --url http://127.0.0.1:30000 --prompt-tokens 101 .py --url http://127.0.0.1:30000 --prompt-tokens 101 4.py --url http://127.0.0.1:30000 --prompt-tokens 101 42.py --url http://127.0.0.1:30000 --prompt-tokens 101 421.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 17.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 170.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 68.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 682.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 100.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 1000.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 12.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches S.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGL.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(C.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413);.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthrough.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#con.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency))..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE`.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNV.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPA.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:41.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413)..py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n##.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-7.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a2552.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a25520.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_t.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sg.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nk.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/       .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Trit.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert G.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output),.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_m.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/        .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                q.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_p.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the P.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table).py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/         .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools:.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n               .py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log,.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, n.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.py --url http://127.0.0.1:30000 --prompt-tokens 101 421 1701 6821 10001 --max-tokens 128\n```\n\n`scripts/serve.sh` is the launcher that produced the published numbers. It sets the environment, applies the\npatch list, launches SGLang and waits for `/health`. The published launch is `--max-running-requests 1`\n(CAMPAIGN.md:413); `--max-running-requests 2` is the concurrency variant, which trades decode speed for\nthroughput (see [Concurrency](#concurrency)). The PLE table lives on NVMe: `PLE` must point at a path on an\nNVMe mount, not a spinning disk (CAMPAIGN.md:413).\n\n## Repository layout\n\n```\nsglang/         the serving patch (qwen4exp-serving-73a255206f.patch) and the layered patch scripts it was built from\npatches/        the serving patch's individual layers (kv_tiers.py, kv_tiers.sh, kv_tiers_validate.py,\n                elastic_ctl.py, elastic_ctl.sh, elastic_ctl_validate.py, elastic_ctl_validate.sh,\n                elastic_ctl_validate.sh.log, sglang_qwen4exp.patch, sglang_qwen4exp.sh)\nkernels/        the Triton kernels: int2_moe.py (int2 expert GEMM), int2_moe_validate.py (its correctness\n                check), int2_moe_validate.log (its output), int2_moe_validate.sh (the runner),\n                int2_moe_validate.sh.log (its output), int2_moe_validate.sh.log (the runner's output)\nassets/         the two serving assets: phase1_state.json (the accepted state) and\n                qwen4exp_ple_ngram_table.json (the PLE n-gram table)\ntools/          the measurement tools: bench.py, ngram_table.py, ngram_table_validate.py,\n                ngram_table_validate.sh, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log,\n                ngram_table_validate.sh.log, ngram_table_validate.sh.log, ngram_table_validate.sh.log",
 "ttft_seconds": 1.7235512390034273,
 "status": "ok",
 "tg_tokens_per_s": 35.7128002834593,
 "decode_seconds_per_token": 0.028001164626207116,
 "individual_token_timing": true,
 "event_intervals_ms": {
  "n": 511,
  "median": 27.514875007909723,
  "p95": 35.13213599217124,
  "mean": 28.001164626207117
 },
 "class": "prose",
 "requested_input_tokens": 2048
}
